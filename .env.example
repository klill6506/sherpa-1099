# Sherpa 1099 Configuration
# Copy this file to .env and fill in your values
# NEVER commit .env to version control

# =============================================================================
# SUPABASE CONFIGURATION
# =============================================================================

# Supabase Project URL
SUPABASE_URL=https://your-project.supabase.co

# Supabase Anon/Public Key (safe to use in frontend for auth only)
SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Service Role Key (SERVER-SIDE ONLY - never expose to frontend!)
# This key bypasses Row Level Security - use only in FastAPI backend
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# =============================================================================
# IRS IRIS API CONFIGURATION
# =============================================================================

# Required: Your IRS-issued Client ID (UUID format)
IRIS_CLIENT_ID=your-client-id-here

# Optional: Path to RSA private key (default: ./IRIS_KEYS/iris_private.key)
# IRIS_PRIVATE_KEY_PATH=./IRIS_KEYS/iris_private.key

# Optional: Key ID matching your JWKS registration (default: iris-a2a-2025)
# IRIS_KEY_ID=iris-a2a-2025

# Optional: Environment - ATS (test) or PROD (default: ATS)
# IRIS_ENVIRONMENT=ATS

# Optional: Override endpoints (usually not needed - defaults to ATS)
# IRIS_AUTH_ENDPOINT=https://ats-api.irs.gov/oauth/token
# IRIS_API_BASE_URL=https://ats-api.irs.gov/iris/v1

# =============================================================================
# IRS TIN MATCHING CONFIGURATION
# =============================================================================

# TIN Matching TCC (Transmitter Control Code) - different from IRIS
# TIN_MATCH_TCC=your-tin-match-tcc

# TIN Matching endpoint (interactive)
# TIN_MATCH_URL=https://tin-matching.irs.gov/api/v1/verify

# =============================================================================
# SMTP EMAIL CONFIGURATION
# =============================================================================

# SMTP server hostname
SMTP_SERVER=smtp.example.com

# SMTP server port (587 for STARTTLS, 465 for SSL)
SMTP_PORT=587

# SMTP authentication credentials
SMTP_USERNAME=your-email@example.com
SMTP_PASSWORD=your-app-password

# From address and name for outgoing emails
SMTP_FROM_EMAIL=noreply@example.com
SMTP_FROM_NAME=Sherpa 1099

# Use STARTTLS (1/true/yes) - recommended for port 587
SMTP_USE_TLS=1

# Use SSL from start (1/true/yes) - use for port 465
SMTP_USE_SSL=0
